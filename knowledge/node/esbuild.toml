[tool]
name = "esbuild"
category = "build"
homepage = "https://esbuild.github.io"
docs = "https://esbuild.github.io/api/"
repo = "https://github.com/evanw/esbuild"
description = "Fast JavaScript and TypeScript bundler"

[detect]
dev_dependencies = ["esbuild"]
ecosystems = ["node"]

[commands]
run = "npx esbuild"

[config]
files = []

[taxonomy]
role = ["build-tool", "compiler"]
function = ["bundling"]
layer = ["frontend"]
